stages:
  - test

test:
  stage: test
  tags:
    - wireguard

  before_script:
    - apt-get update
    - apt-get install -y openssh-client

  script:
    - hostname
    - ssh oskar@10.10.0.5 hostname